{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--headless", "--isolated", "--output-dir", ".playwright-mcp"],
      "description": "Headless browser driver for UI verification. Lets the agent navigate/click/fill/screenshot the running app to verify it works. Only use these tools when verifying UI scenarios. Requires Chromium installed in the runtime (npx playwright install chromium, one-time host setup)."
    }
  }
}
