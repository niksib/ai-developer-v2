{
  "$comment": "Model strategies the eval harness compares. `runner` picks the adapter in runners/. `models.main` sets the brain's model for the whole run; per-subagent models are pinned in .claude/agents/* frontmatter. Decide on data: success rate > cost > latency.",
  "strategies": {
    "opus-brain": {
      "runner": "standalone",
      "description": "Main session on Opus for the whole task; subagents at their frontmatter tiers (coder=sonnet, review=opus, docs=haiku).",
      "models": { "main": "opus" }
    },
    "sonnet-brain": {
      "runner": "standalone",
      "description": "Main session on Sonnet — the cost hypothesis for S/M-tier tasks; review subagent still opus per frontmatter.",
      "models": { "main": "sonnet" }
    }
  }
}
